# Client (embedded at build time). Use wss:// in production; never put secrets in VITE_* values.
VITE_PVP_WS_URL=

# Relay host (server/pvp-server.mjs). PORT is preferred by most hosts; PVP_PORT is a local alias.
PORT=2567
PVP_PORT=2567
PVP_SCORE_LIMIT=15
